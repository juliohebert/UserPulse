{
  "name": "userpulse",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "dev":         "concurrently -n server,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "build":       "npm run build --prefix server && npm run build --prefix web",
    "start":       "npm run db:up && npm run db:migrate && npm run db:seed && npm run dev",
    "db:up":       "docker compose up -d --wait",
    "db:migrate":  "npm run db:migrate:deploy --prefix server",
    "db:seed":     "npm run db:seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
